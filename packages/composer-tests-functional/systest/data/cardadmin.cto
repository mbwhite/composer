namespace systest.cardadmin

participant SampleParticipant identified by email {
  o String email
  o String firstName
  o String lastName
  o String userProfile optional
}

transaction UpdateProfile {
  o String update
}